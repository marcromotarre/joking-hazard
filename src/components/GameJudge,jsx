/*

          {hasAllPlayersPlayedCard && (
            <GameBoardJudgement
              firstCardId={deckRandomCardId}
              secondCardId={judgeCardId}
              thirdCardId={judgeCardId}
            />
          )}

*/